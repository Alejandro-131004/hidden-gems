# URGENT

 1. Data is pretty inconsistent. Needs grouping, NaN removal or mitigation... and so much more
    1. manually grouping by table roles is inconsistent (needs to be marked as deprecated), but so is grouping by similar NaN column structure. Maybe we can try to use only the research-based columns first.
 2. For AI preparation, will require team and player anonymization (we can combine & create unique IDs, but how? they can change teams between seasons, and also grow taller, there can also be more than one with the same name on the same team with relatively the same height)